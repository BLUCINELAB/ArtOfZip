{
  "name": "anton-likht-portfolio",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "lint": "node scripts/check-site.mjs",
    "test": "npm run build && node --test",
    "verify": "npm run lint && npm test",
    "preview": "node scripts/serve.mjs dist 4173"
  }
}
